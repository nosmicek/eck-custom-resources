---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: inferenceendpoints.es.eck.github.com
spec:
  group: es.eck.github.com
  names:
    kind: InferenceEndpoint
    listKind: InferenceEndpointList
    plural: inferenceendpoints
    singular: inferenceendpoint
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: InferenceEndpoint is the Schema for the inferenceendpoints API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: InferenceEndpointSpec defines the desired state of InferenceEndpoint
            properties:
              body:
                type: string
              targetInstance:
                properties:
                  name:
                    type: string
                type: object
              taskType:
                description: |-
                  TaskType forms part of the endpoint's path in Elasticsearch, so it cannot be
                  changed without recreating the endpoint.
                enum:
                - sparse_embedding
                - text_embedding
                - rerank
                - completion
                - chat_completion
                - embedding
                type: string
                x-kubernetes-validations:
                - message: taskType is immutable
                  rule: self == oldSelf
            required:
            - body
            - taskType
            type: object
          status:
            description: InferenceEndpointStatus defines the observed state of InferenceEndpoint
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
