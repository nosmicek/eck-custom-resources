---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: queryrulesets.es.eck.github.com
spec:
  group: es.eck.github.com
  names:
    kind: QueryRuleset
    listKind: QueryRulesetList
    plural: queryrulesets
    singular: queryruleset
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: QueryRuleset is the Schema for the queryrulesets API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: QueryRulesetSpec defines the desired state of QueryRuleset
            properties:
              body:
                type: string
              targetInstance:
                properties:
                  name:
                    type: string
                type: object
            required:
            - body
            type: object
          status:
            description: QueryRulesetStatus defines the observed state of QueryRuleset
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
